# Core runtime
PyMuPDF>=1.24.10         # 'fitz' module
pillow>=10.3.0           # PIL / image ops
pandas>=2.2.2
numpy>=1.26.4
pytesseract>=0.3.10      # Python bridge to the Tesseract OCR engine
pyyaml>=6.0.1
matplotlib>=3.8.4
gradio>=4.41.0           # UI

# ── Optional: enable NER with Hugging Face (only if you plan to use dslim/bert-base-NER) ──
transformers>=4.44.2
torch>=2.3.1           # required by transformers for most pipelines (CPU build is fine)

# Note:
# - You must install the Tesseract OCR engine separately (system package), not via pip.
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   macOS (Homebrew): brew install tesseract
#   Debian/Ubuntu:    sudo apt-get install tesseract-ocr
